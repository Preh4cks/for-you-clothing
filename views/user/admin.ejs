<!DOCTYPE html> 
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="stylesheets/index.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <title>Admin</title>
    <style>
        form {
            padding: 20px;
            border: 1px solid black;
            width: 400px;
            margin: 20px auto;
        }
        input, textarea {
            width: 100%;
            padding: 10px 20px;
            border: 1px solid black;
            margin-bottom: 10px;
            background-color: transparent;
        }
        
        .category_list {
            margin: auto;
            width: 400px;
            border: 1px solid black;
        }
        .category_list > p {
            padding: 10px 20px;
            border-bottom: 1px solid black;
        }
    </style>
</head>
<body>
    <script>
        function recurse() {
            let password = prompt("Who is the Father of Modern Robotics?: ", "").toLowerCase();
            
            if(password == "angels burger" || password == "engelberger" || password == "joseph engelberger" || password == "joseph" || password == "joseph fredrick engelberger") {
                location.href = "/admin/products";
            } else {
                recurse();
            }
        }

        recurse();

    </script>
    <!-- <a href="/admin/products" class="black_button">PRODUCTS</a> -->
</body>
</html>